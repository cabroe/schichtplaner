# Schichtplaner Environment Configuration
# Copy this file to .env and adjust values as needed

# Port on which the Go backend server will run
SERVER_PORT=3000

# Environment mode: "dev" for development (proxy to Vite), "prod" for production (serve static files)
ENV=dev
